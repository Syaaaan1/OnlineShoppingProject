@*@model DbOptions.Models.user_entity

@{
    ViewData["Title"] = "User Information";
}

<div class="user-info-container vh-50">
    <div class="container">
        <h2 class="text-center mt-4">Welcome @Model.Username!</h2>

        <div class="user-details mt-4">
            <h4>User Details:</h4>
            <ul>
                <li><strong>Username:</strong> @Model.Username</li>
                <li><strong>Email:</strong> @Model.Email</li>
                <li><strong>Phone Number:</strong> @Model.phoneNumber</li>
                <!-- Add other user details as needed -->
            </ul>
        </div>
    </div>
</div>

<div class="user-orders-container vh-50">
    <div class="container">
        <h4 class="text-center mt-4">Your Orders:</h4>
        <table class="table table-bordered mt-4">
            <thead>
                <tr>
                    <th>OrderID</th>
                    <th>Order Date</th>
                    <th>Status</th>
                    <th>Total Amount</th>
                    <th>Delivery Address</th>
                    <th>Payment Method</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var order in Model.orders)
                {
                    <tr>
                        <td>@order.Id</td>
                        <td>@order.OrderDate.ToShortDateString()</td>
                        <td>@order.Status</td>
                        <td>@order.TotalAmount.ToString("C")</td>
                        <td>@order.DeliveryAddress</td>
                        <td>@order.PaymentMethod</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<form asp-action="Logout" method="post" class="container mt-4">
    <button type="submit" class="btn btn-danger btn-block" asp-controller="Login" asp-action="Logout">Logout</button>
</form>

<!-- Ваш CSS остается без изменений -->

<style>
    body {
        background-color: #f8f9fa;
    }

    .container {
        padding: 20px;
    }

    .user-info-container, .user-orders-container {
        width: 100%;
        background-color: #fff;
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
        margin-bottom: 20px;
    }

    .vh-50 {
        height: 50vh;
    }

    .user-info-container {
        padding: 20px;
    }

    .user-orders-container {
        padding: 20px;
    }

    table {
        width: 100%;
        margin-top: 20px;
    }

    th, td {
        text-align: left;
        padding: 8px;
    }

    th {
        background-color: #f2f2f2;
    }

    .btn-danger {
        background-color: #dc3545;
        border-color: #dc3545;
        transition: background-color 0.3s, transform 0.3s;
    }

        .btn-danger:hover {
            background-color: #c82333;
            border-color: #bd2130;
            transform: translateY(-3px);
        }
</style>*@
